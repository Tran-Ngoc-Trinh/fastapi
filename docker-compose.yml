version: '3.7'
services:
  # frontend:

  api:
    container_name: api
    build: ./backend
    ports:
      - "8000:8000"
    working_dir: /app
    volumes:
      - ./:/app
    depends_on:
      - mysql
  mysql:
    image: mysql:8.0.28
    container_name: mysql
    ports:
      - "3307:3306"
    hostname: db
    env_file:
      - ./.env
    restart: always
    volumes:
      - database:/var/lib/mysql
volumes:
  database:

